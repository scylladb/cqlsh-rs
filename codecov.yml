coverage:
  precision: 2
  round: down
  range: "40...100"
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        informational: true

codecov:
  notify:
    after_n_builds: 5
